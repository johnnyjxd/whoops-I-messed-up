#include <iostream>
using namespace std;

    int main() {
        char res= y;
        int usercurrency;
        int final_result = usercurrency * 2 - 2 + 55 - 32 + 10;
        cout << "enter an ammount of us dollar: ";
        cin >> usercurrency;
        cout << "getting ammount ready type y when ready";
        if (res==y) {
            cout << final_result;
        }
    }